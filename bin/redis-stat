#!/usr/bin/env ruby

require 'rubygems'
require 'redis-stat'

options = RedisStat::Option.parse ARGV
rs = RedisStat.new options
begin
  rs.start $stdout
rescue Exception
  raise if options[:verbose]
end
